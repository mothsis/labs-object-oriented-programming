<Window x:Class="Lab19_Individual.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Индивидуальное задание 19.3.2"
        Height="350" Width="450">

    <Window.Resources>

        <!-- 1. Стиль основной кнопки -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2C5F99"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 2. Стиль кнопки предупреждения -->
        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E94E4E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C63C3C"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 3. Стиль поля ввода -->
        <Style x:Key="StyledTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#F0F0F0"/>

            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="#FFF7CC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 4. Стиль информационного текста -->
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

    </Window.Resources>

    <!-- Здесь должен быть основной контент окна -->
    <Grid>
        <!-- Пример использования стилей -->
        <StackPanel Margin="20">
            <Button Style="{StaticResource PrimaryButtonStyle}" 
                    Content="Основная кнопка"/>

            <Button Style="{StaticResource WarningButtonStyle}" 
                    Content="Кнопка предупреждения"/>

            <TextBox Style="{StaticResource StyledTextBox}" 
                     Text="Текст для ввода"/>

            <TextBlock Style="{StaticResource InfoTextStyle}"
                       Text="Информационный текст"/>
        </StackPanel>
    </Grid>
</Window>